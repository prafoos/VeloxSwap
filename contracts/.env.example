# Arc Testnet RPC endpoint
ARC_TESTNET_RPC_URL=https://rpc.testnet.arc.network

# The private key of your deployer wallet (without the 0x prefix)
# DO NOT commit the actual .env file containing your private key!
PRIVATE_KEY=your_private_key_here
